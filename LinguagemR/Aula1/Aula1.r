# Aula 1, Aprendendo a Sintaxe da Linguagem

# Fórmula de Celsius para Fahrenheit
(f <- (25 * (9 / 5)) + 32)

# Fórmula para calcular distância entre dois pontos
x <- 1
x2 <- 4
y <- 2
y2 <- 6

(d <- sqrt((x2 - x)^2 + (y2 - y)^2))

# Cálculo de Báskara
a <- 1
b <- -3
c <- -4

(x <- -b + (sqrt(b^2 - (4 * a * c)) / 2 * a))
(x2 <- -b - (sqrt(b^2 - (4 * a * c)) / 2 * a))
